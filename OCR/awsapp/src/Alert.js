import React from 'react'
import { withAlert } from 'react-alert'

const AppTest = (props) => {


    // testClick = () => {
    //     this.props.testClick()
    // }

    return (
        <button onClick={this.props.alert.show()} >
            Show Alert
        </button>
    )
}

export default AppTest